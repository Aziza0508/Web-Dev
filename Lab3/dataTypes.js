let n = 123;
n = 12.345;
// number  data types include everything

alert( 1 / 0 );
// Infinity

alert( "not a number" / 2 );
// NaN not a number prikin'

console.log(9007199254740991 + 1);
// bit int

let str = "Hello";
let str2 = 'Single quotes are ok too';
let phrase = `can embed another ${str}`;
// Strings 

let name1 = "John";
alert( `Hello, ${name}!`)
// embed a variable, or an expression


let isGreater = 4 > 1;
alert( isGreater );
//boolean



// task
let name = "Aziza";

alert( `hello ${1}` ); // hello 1

alert( `hello ${"name"}` ); // hello name

alert( `hello ${name}` ); // hello Aziza